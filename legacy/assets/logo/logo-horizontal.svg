<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 480 160" role="img" aria-label="智御无疆 Kaup Horizontal Lockup — left-aligned (app header / nav bar variant)">
  <title>智御无疆 / Kaup — Horizontal Lockup (左对齐) — Kaup Chevron (15° tilt) + amber dot, transparent background</title>

  <style>
    /* Amber dot — breathing light: brightness only, size and position unchanged
       (approved motion exception, 2026-09-11). Declared inside the SVG so it also
       runs in &lt;img&gt; / &lt;use&gt; contexts. */
    .kaup-dot { animation: kaup-dot-breathe 4.2s cubic-bezier(0.37, 0, 0.63, 1) infinite; }
    @keyframes kaup-dot-breathe {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.3; }
    }
    @media (prefers-reduced-motion: reduce) {
      .kaup-dot { animation: none; }
    }
  </style>

  <!-- Mark: chevron + 琥珀点, 贴边左对齐 (mark translate(0, 30) 紧贴 viewBox 左缘), 视觉中心 y=80 (viewBox 中线) -->
  <g transform="translate(0 30)">
    <g transform="rotate(-15 50 50)">
      <path d="M 15 15 C 25 20, 55 35, 80 50 C 55 65, 25 80, 15 85 L 40 85 C 50 78, 75 65, 85 50 C 75 35, 50 22, 40 15 Z" fill="#0E2A47"/>
      <circle class="kaup-dot" cx="76" cy="50" r="5" fill="#E89B3C"/>
    </g>
  </g>

  <!-- 中文 wordmark, 共享左缘 (text-anchor: start), 距 mark 旋转后右缘 ~21px -->
  <text x="100" y="78"
        style="font-family: 'PingFang SC', 'HarmonyOS Sans SC', 'Source Han Sans SC', 'Noto Sans SC', '微软雅黑', sans-serif; font-size: 48px; font-weight: 600; fill: #0E2A47; text-anchor: start; letter-spacing: 0.18em;">智御无疆</text>

  <!-- 英文 wordmark, 共享左缘, 与 CN 共享 x=100 -->
  <text x="100" y="110"
        style="font-family: Inter, -apple-system, 'SF Pro Display', system-ui, sans-serif; font-size: 13px; font-weight: 500; fill: #5B6B7E; text-anchor: start; letter-spacing: 0.4em;">Kaup</text>
</svg>
